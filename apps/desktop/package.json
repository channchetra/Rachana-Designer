{
  "name": "@rachana/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Rachana Designer desktop app — Tauri shell around the shared core. Skeleton: see README.md for the adapter plan and remaining work.",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "dev": "echo \"Not wired up yet — see README.md (requires the Rust toolchain and the Tauri CLI)\" && exit 1",
    "build": "echo \"Not wired up yet — see README.md\" && exit 1"
  },
  "dependencies": {
    "@rachana/core": "*"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  }
}
